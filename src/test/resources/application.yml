server:
 port: 9091
management:
 endpoint:
  metrics:
   enabled: true

spring:
 datasource:
  url: jdbc:h2:mem:skillsdb
  username: sa
  password: password
 jpa:
  database-platform: org.hibernate.dialect.H2Dialect
  hibernate.ddl-auto: create
 h2:
  console:
   enabled=true: